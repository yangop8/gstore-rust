/target
**/*.rs.bk
Cargo.lock.orig
# gStore database directories produced by gbuild (suffix .db)
*.db/
/scratch

# OMC orchestration state
/.omc/

# Claude Code agent worktrees / local state
.claude/

# Local gNLQA config (holds API keys) — commit gnlqa.conf.example instead
gnlqa.conf

# CCB control-plane runtime state
/.ccb/
